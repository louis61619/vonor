{
  "name": "root",
  "packageManager": "pnpm",
  "private": true,
  "devDependencies": {
    "commitizen": "^4.2.4",
    "cz-lerna-changelog": "^2.0.3",
    "lerna": "^4.0.0"
  },
  "scripts": {
    "commit": "cz",
    "docs": "npm run docs:dev --workspace docs",
    "serve": "npm workspace @vonor/admin serve",
    "docs:dev": "pnpm run -C docs dev"
  },
  "workspaces": [
    "packages/*",
    "docs",
    "example"
  ],
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-lerna-changelog"
    }
  }
}
